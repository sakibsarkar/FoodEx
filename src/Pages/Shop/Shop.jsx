import "./Shop.css";
import { useParams } from "react-router-dom";

const Shop = () => {
    const { vendor_id } = useParams()

    

    return (
        <div className="shopContainer">



        </div>
    );
};

export default Shop;